@extends('layouts.app')
@section('title','Order #'.$order->id.' · MyStore')
@section('content')
<section class="page-hero"><div class="container"><div class="breadcrumbs">Account / Orders / #{{ $order->id }}</div><h1>Order #{{ $order->id }}.</h1><p>Placed {{ $order->created_at->format('F d, Y · h:i A') }}.</p></div></section>
<section class="section"><div class="container"><div class="split"><div class="page-card"><div class="eyebrow">Items</div>@foreach($order->items as $item)<div style="display:flex;gap:14px;align-items:center;padding:16px 0;border-bottom:1px solid var(--line)"><img src="{{ $item->product->image ?: 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=300&q=80' }}" style="width:70px;height:70px;border-radius:14px;object-fit:cover"><div style="flex:1"><strong>{{ $item->product->name }}</strong><div style="color:var(--muted);font-size:11px">Qty {{ $item->quantity }}</div></div><strong>${{ number_format($item->price*$item->quantity,2) }}</strong></div>@endforeach</div><div class="page-card"><div class="eyebrow">Delivery</div><h2>{{ $order->name }}</h2><p style="color:var(--muted)">{{ $order->address }}<br>{{ $order->city }}, {{ $order->state }} {{ $order->zip }}<br>{{ $order->phone }}</p><div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:20px"><span class="status info">{{ $order->status }}</span><span class="status">{{ $order->payment_status }}</span></div><div style="margin-top:25px;display:grid;gap:8px"><div style="display:flex;justify-content:space-between"><span>Subtotal</span><span>${{ number_format($order->subtotal,2) }}</span></div><div style="display:flex;justify-content:space-between"><span>Shipping</span><span>${{ number_format($order->shipping,2) }}</span></div><div style="display:flex;justify-content:space-between;border-top:1px solid var(--line);padding-top:12px;font-size:18px"><strong>Total</strong><strong>${{ number_format($order->total,2) }}</strong></div></div></div></div></div></section>
<x-store-discovery-sections />
@endsection
